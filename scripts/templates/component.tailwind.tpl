{{tsIgnore}}import { State{{importExtras}} } from 'ura';

function {{Name}}(props{{propsType}}){{vdomType}} {
  return (
    <div className="p-4">
      <h1 className="text-xl font-semibold text-text">{{Name}} component</h1>
    </div>
  );
}

export default {{Name}}
